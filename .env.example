# Environment Configuration for ComfyUI Docker
# Copy this to .env to customize storage paths

# Shared storage path (contains models and input)
COMFY_SHARED_PATH=./storage/shared

# Instance-specific storage path
COMFY_INSTANCES_PATH=./storage/instances

# User/Group IDs for container ownership
PUID=1000
PGID=1000

# Optional: Override default images
# COMFY_IMAGE=ghcr.io/pixeloven/comfyui-docker/core:cuda-latest

# Note: All instances now use the same storage pattern:
# - Single instance: uses 'comfy' as default instance ID
# - Multi-instance: specify different instance IDs in compose files