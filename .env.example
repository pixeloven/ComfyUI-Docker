#=====================================================================#
#                       Server Configuration                          #
#=====================================================================#

PUID=1000
PGID=1000

# Default driver is cpu [cpu|nvidia]
DEVICE_DRIVER=nvidia

#=====================================================================#
#                       Automatic1111 Configuration                   #
#=====================================================================#

AUTO_WEBUI_PORT=7860
AUTO_CLI_ARGS="--allow-code --medvram --xformers --enable-insecure-extension-access --api"
# AUTO_CLI_ARGS="--no-half --precision full --allow-code --enable-insecure-extension-access --api"

#=====================================================================#
#                       ComfyUI Configuration                         #
#=====================================================================#

COMFY_WEBUI_PORT=8188

# ComfyUI CLI Arguments
# For GPU mode (default):
COMFY_CLI_ARGS=
# For CPU-only mode, uncomment the line below:
# COMFY_CLI_ARGS="--cpu"
# For CPU with specific optimizations:
# COMFY_CLI_ARGS="--cpu --force-fp16"
# For low VRAM GPU mode:
# COMFY_CLI_ARGS="--lowvram"
# For very low VRAM GPU mode:
# COMFY_CLI_ARGS="--novram"
