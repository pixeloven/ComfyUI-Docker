#=====================================================================#
#                       Server & Setup Configuration                  #
#=====================================================================#

# User/Group ID for file ownership on the host (defaults to 1000)
# The container process can run as any user (Swarm/K8s supported)
PUID=1000
PGID=1000

#=====================================================================#
#                       ComfyUI Configuration                         #
#=====================================================================#

COMFY_PORT=8188

# Individual path configuration (used by core-cuda service)
COMFY_CUSTOM_NODE_PATH=./data/custom_nodes
COMFY_INPUT_PATH=./data/input
COMFY_MODEL_PATH=./data/models
COMFY_OUTPUT_PATH=./data/output
COMFY_TEMP_PATH=./data/temp
COMFY_USER_PATH=./data/user

# Legacy single data path (used by complete-cuda and core-cpu services)
COMFY_DATA_PATH=./data