# Provides common dependencies and pre-built wheels for consistent builds

# Additional dependencies
accelerate>=1.10
onnxruntime-gpu>=1.22
diffusers>=0.34

# Custom node optimizations - frequently used dependencies baked in for faster startup
opencv-python-headless>=4.12
scipy>=1.16
matplotlib>=3.10

# PuLID / IPAdapter
facexlib>=0.3
insightface>=0.7

# ComfyUI Impact Pack dependencies
piexif>=1.1
ultralytics>=8.3

# RMBG and AILab Segmentation
timm>=1.0
groundingdino-py>=0.4.0

# HiDiffusion
# hidiffusion>=0.1.10 # Can't find how to use this or a node that supports it.

# Core acceleration libraries (pre-built wheels)
# triton>=3.4

# SageAttention 2.2.0 + SageAttn3 1.0.0 (pre-built) - CUDA 12.9.1 + Python 3.12 (build.15)
# Provides: sageattention (SM80+), sageattn3 (Blackwell/SM100+)
https://github.com/pixeloven/SageAttention/releases/download/v2.2.0-build.16/sageattention-2.2.0-290.129-cp312-cp312-linux_x86_64.whl
https://github.com/pixeloven/SageAttention/releases/download/v2.2.0-build.16/sageattn3-1.0.0-290.129-cp312-cp312-linux_x86_64.whl

PyYAML

# @todo Review all the major custom nodes on the list we'd want to support. 
# Break down all shared deps and focus on the most common. 
# In addition consider use cases that are complex to setup such as ipadapter
# insightface is old is there a way to get a newer build?