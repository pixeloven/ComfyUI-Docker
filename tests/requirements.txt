# Essential test dependencies for ComfyUI Docker tests
# Focused on critical testing needs for single developer maintenance
pytest>=7.0.0
requests>=2.28.0  # For HTTP requests to test services
docker>=6.0.0  # For Docker API interactions
