The ComfyUI instance your tools are connected to has an image named `harness-input.png` in its input directory.

Build a workflow that loads that image, scales it to exactly 256×256 pixels, inverts its colours, and saves the result with the filename prefix `t1_`. Use only ComfyUI's built-in nodes; no model checkpoint is installed, and none is needed.

Validate the workflow if your tools can, run it, and wait until it has finished. Then report the filename of the saved image.
